# Copy to .env.local and fill in your values

# PostgreSQL connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/revera_clinic

# Set to "true" if your PostgreSQL host requires SSL (e.g. remote/cloud servers)
DATABASE_SSL=false
